# 進化必要枚数計算ツール 仕様書(フェーズ1)

最終更新: 2026-07-14
ステータス: 実装済み・v1.1修正反映済み(2026-07-14)
対象サイト: memento.musoudc.com
ファイル名(案): `evolution-calc.html`(ルート直下、dedicated-weapon-calc.html と同列)

---

## 1. 概要

現在レアリティから目標レアリティまでに必要な同名キャラ・同属性素材の枚数を計算し、
所持数を差し引いた不足数を表示する単一ページツール。

- 想定ユーザー: 無課金・微課金の初心者〜中級者
- 初期SRキャラを素材に回せるほどキャラが余っている層は対象外
- ダイヤ換算・到達日数の試算は本ツールに持たせない(ガチャツール側の守備範囲)

## 2. スコープ

### フェーズ1(本仕様の範囲)

- 起点は SR / SR+ / SSR / SSR+ / UR / UR+ / LR / LR+1〜LR+4 から選択可能(N・R起点は対象外)
- 目標レアリティ: SR+ / SSR / SSR+ / UR / UR+ / LR / LR+1〜LR+5
- 計算は「現在レアリティ→目標レアリティ」の残りステップのみ合算する
- 目標レアリティは現在レアリティより上のみ選択可能にする
- 必要枚数を「同名キャラ」と「同属性素材」に分けて表示
- 同属性素材は R キャラ何枚から育成する必要があるかまで分解して表示
- 所持数入力(同名・同属性素材・R素材)から不足数を算出
- ガチャツールへの受け渡し口(URLパラメータ)の実装(§6)
- localStorage 保存は不要(入力が少なく毎回入力で成立するため)

### フェーズ2(本仕様の範囲外・実装しない)

- 無料入手ペースからの到達日数試算
- 進化素材の不足状況を考慮したピックアップ/運命ガチャの優先度提案
  (→ ガチャツール側で実装。IDEAS.md カテゴリ5へ追記)
- ガチャツール側での `need` パラメータ受け取り実装

## 3. 入力項目

| 項目 | 形式 | 備考 |
|---|---|---|
| 現在レアリティ | セレクト | SR〜LR+4 |
| 目標レアリティ | セレクト | SR+〜LR+5 |
| 所持: 同名キャラ | 数値 | 育成対象本体を除いた枚数 |
| 所持: 同属性SR+素材 | 数値 | 必要段階が異なるため SSR+ と分離(as-built) |
| 所持: 同属性SSR+素材 | 数値 | 同上 |
| 所持: R+ キャラ(同属性) | 数値 | R換算で3枚相当 |
| 所持: R キャラ(同属性) | 数値 | 素材化前の R 枚数 |

### R不足の計算仕様(2026-07-14 確定・v1.1)

- 所持SSR+素材の充当は必要数まで。**余剰SSR+はSR+不足に充当しない**(SSR+はSR+に戻せないため)
- 余剰SR+素材のみ 18枚/体 でR充当可能数に加算(上方向充当は追加R込みの換算として正確)
- R+素材は1体=R3枚としてR充当可能数に加算
- `creditR = ownedR + ownedRplus × 3 + max(0, ownedSrPlus − needSrPlus) × 18`
- `rShortage = max(0, srPlusShortage×18 + ssrPlusShortage×72 − creditR)`

- 属性選択は計算に影響しないため不要。「同属性で数える」旨を注記する
- 「進化元含む総数」の注記は、現在レアリティがSRのときのみ表示する

## 4. 出力項目

1. 必要な同名キャラ 総数 / 不足数
2. 必要な同属性素材 総数 / 不足数(段階別内訳つき)
3. 同属性素材を作るために必要な R キャラ換算枚数 / 不足数(R充当可能数を加味)
4. 不足がある場合のみ「ガチャ計算ツールで必要ダイヤを見る」ボタン(§6)

不足数が 0 以下の場合は「素材は足りています」を明示する。

### 表示改善(v1.1)

- 結果3カードの見出しは数字との対応が分かるように本文より大きく表示する
- 内訳は以下の2グループに分離する
  - 同名(育成キャラ自身): 同名SR+素材
  - 同属性(素材キャラ): 同属性SR+素材 / 同属性SSR+素材 / R+・R充当可能数

## 5. 進化テーブル(共通データ・確定値)

**数値は 2026-07-14 に夢爽が確認済みの確定値。ミコトは変更しないこと。**

- 対象は通常4属性(藍・紅・翠・黄)のみ。**天・冥属性は本ツール対象外**
  (進化素材が同名キャラのみで体系が異なる。ガチャツール側でカバー済み)。
  ツール上部に「天冥属性は対象外」の注記を表示する。

共通データとして `data/evolution-table.json` に切り出し、
本ツールと将来のガチャツール連携の双方から参照する(IDEAS.md カテゴリ15「更新箇所を1か所に集約」に準拠)。

```json
{
  "updatedAt": "2026-07-14",
  "note": "通常4属性(藍・紅・翠・黄)専用。天冥は対象外。",
  "steps": [
    { "from": "SR",   "to": "SR+",  "materials": { "sameCharSR": 1 } },
    { "from": "SR+",  "to": "SSR",  "materials": { "sameAttrSRplus": 2 } },
    { "from": "SSR",  "to": "SSR+", "materials": { "sameCharSRplus": 1 } },
    { "from": "SSR+", "to": "UR",   "materials": { "sameAttrSSRplus": 1 } },
    { "from": "UR",   "to": "UR+",  "materials": { "sameAttrSSRplus": 1 } },
    { "from": "UR+",  "to": "LR",   "materials": { "sameCharSRplus": 2 } },
    { "from": "LR",   "to": "LR+1", "materials": { "sameCharSRplus": 1 } },
    { "from": "LR+1", "to": "LR+2", "materials": { "sameCharSRplus": 1 } },
    { "from": "LR+2", "to": "LR+3", "materials": { "sameCharSRplus": 1 } },
    { "from": "LR+3", "to": "LR+4", "materials": { "sameCharSRplus": 1 } },
    { "from": "LR+4", "to": "LR+5", "materials": { "sameCharSRplus": 2 } }
  ],
  "conversion": {
    "sameCharSRplus_to_sameCharSR": 2,
    "Rplus_to_R": 3,
    "sameAttrSRplus_to_R": 18,
    "sameAttrSSRplus_to_R": 72,
    "R_to_SR_reference": 9
  }
}
```

### 換算ルール

- 同名SR+ 1体 = 同名SRキャラ 2枚
- R+キャラ 1体 = 同属性Rキャラ 3枚
- 同属性SR+素材 1体 = 同属性Rキャラ 18枚
- 同属性SSR+素材 1体 = 同属性Rキャラ 72枚
- RキャラはSSR+までしか進化できない(UR不可)

### 検算用の累計値(QAで使用)

- SR→LR: 同名SR 8枚(進化元含む)、同属性R 180枚(SR+×2=36、SSR+×1×2回=144)
- SR→LR+5: 同名SR 20枚(進化元含む)
- SSR→LR: 同名SR+3体(同名SR6枚)、同属性SSR+2体(同属性R144枚)、同属性SR+0体
- UR+→LR: 同名SR+2体(同名SR4枚)、同属性素材なし

### 目標レアリティ範囲について

当初フェーズ1は SR+〜LR を対象としていたが、確定値が LR+5 まで揃ったため **SR+〜LR+5** に拡張した(§2・§3は修正済み)。

## 6. ガチャツール連携(受け渡し口)

- 不足する同名キャラ数を URL パラメータで渡す:
  `gacha-calc.html?need={不足枚数}`
- フェーズ1では**リンクを張るのみ**。ガチャツール側の受け取り実装はフェーズ2
- localStorage 経由は採用しない(壊れにくさと単純さを優先)
- 前例: dedicated-weapon-calc.html の「必要魔水晶をガチャ計画へ反映」と同パターン

## 7. UI 方針

- 1画面構成。入力ブロック → 計算結果ブロックの縦並び
- モバイル前提(既存ツールと同様)
- 計算はボタン押下または入力即時のどちらでも可(既存ツールの流儀に合わせる)
- 外部CDN依存は追加しない

### UI改善(v1.2)

- 数値入力欄は初期値を空欄にし、`placeholder="0"` を表示する。空欄は0として計算する。
- 所持数入力は2カラムグリッドで配置する。
  - 1行目: 所持 同名SRキャラ / 所持 同属性SR+素材
  - 2行目: 所持 Rキャラ(同属性) / 所持 R+キャラ(同属性)
  - 3行目: 所持 同属性SSR+素材 / 空き
- レアリティ表記はCSS変数で色を管理する。
  - `--color-rarity-r`: R / R+
  - `--color-rarity-sr`: SR / SR+
  - `--color-rarity-ssr`: SSR+
- 同名系と同属性系の見出し色はCSS変数で分ける。
  - `--color-group-same`: 同名(育成キャラ自身)
  - `--color-group-attr`: 同属性(素材キャラ)
- 再計算ボタンは削除する。入力変更・レアリティ変更による即時計算を維持する。

## 8. QA 観点(最低限)

- 目標レアリティ全パターンで必要枚数がテーブル通りか
- 所持数 > 必要数 のとき不足 0 表示になるか
- 所持数に負数・空欄・巨大値を入れたときの挙動
- R+入力に負数・空欄・巨大値を入れたとき、既存入力と同じく0扱い・上限999999になるか
- `?need=` の値が不足数と一致するか
- 現在=目標、現在>目標の選択が発生しないか
- 現在SSR起点時の `?need=` が同名SR6枚と一致するか
- 余剰SSR+をSR+不足へ充当しないR不足計算が退行していないか

## 9. IDEAS.md への追記案(夢爽が反映)

- カテゴリ5: `- [ ] 進化素材の不足状況を考慮したガチャ優先度提案(進化ツールから need を受け取る)`
- カテゴリ6: 該当項目(必要枚数計算・不足数表示)を `[~]` へ更新
- カテゴリ15: `- [ ] 進化テーブルを共通データ(evolution-table.json)として切り出し、進化ツール・ガチャツールで共有`
